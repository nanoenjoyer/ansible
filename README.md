
```sh
sudo apt update && sudo apt install -y git ansible
```
```sh
ansible-pull -U https://github.com/nanoenjoyer/ansible.git
```
> -U is short for URL, this looks for local.yml in root of repo directory.
