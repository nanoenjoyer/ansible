---

- hosts: all
  become: true
  tasks:

  - name: Install apache2 and nginx packages
    apt:
      update_cache: true
      name:
        - apache2
        - nginx
        - screenfetch
        - git
    when: ansible_distribution == "Ubuntu"
